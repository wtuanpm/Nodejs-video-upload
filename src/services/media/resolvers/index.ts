import { Resolvers } from '@graphql/types/generated-graphql-types';
import { Media, ScreenShot, VideoProfile } from './media';

export const mediaResolvers: Resolvers = {
  Media,
  ScreenShot,
  VideoProfile,
};
